package com.autocar.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Metallic gold accent palette (matched to logo) ──
val Gold = Color(0xFFD4AF37)          // classic metallic gold
val GoldLight = Color(0xFFE8D08C)     // warm champagne highlight
val GoldDark = Color(0xFF996515)      // deep bronze
val GoldMuted = Color(0xFFBFA04E)     // brushed gold

// ── Backgrounds ──
val Black = Color(0xFF0A0A0A)
val SurfaceDark = Color(0xFF141418)
val SurfaceVariantDark = Color(0xFF1E1E24)
val CardDark = Color(0xFF1A1A20)

// ── Text ──
val OnDarkPrimary = Color(0xFFE8E8E8)
val OnDarkSecondary = Color(0xFFB8B8C0)   // brighter so it's readable on dark

// ── Status indicators ──
val StatusGreen = Color(0xFF4CAF50)
val StatusRed = Color(0xFFF44336)
val StatusYellow = Color(0xFFFFC107)
